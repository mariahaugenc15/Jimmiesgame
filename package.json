{
  "name": "locked-in",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "pnpm --filter @lockedin/web dev",
    "dev:server": "pnpm --filter @lockedin/server dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "db:migrate": "pnpm --filter @lockedin/server db:migrate",
    "db:seed": "pnpm --filter @lockedin/server db:seed"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.0",
  "pnpm": {
    "overrides": {
      "drizzle-orm": "0.36.4"
    }
  }
}
